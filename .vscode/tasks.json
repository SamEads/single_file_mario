{
    "version": "2.0.0",
    "tasks": [
        { // build task
            "label": "compile and run mario",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build -j 16",
            "args": [
            ],
            "group": "build"
        },
        { // edit task
            "label": "compile and run editor",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build -j 16",
            "group": "build"
        }
    ]
}